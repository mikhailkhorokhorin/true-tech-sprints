# Task 4. Monitoring

Stand up a VMSingle + VMAgent pair (remoteWrite into VMSingle), scrape a demo app's `/metrics` via a VMServiceScrape, wire VMSingle into Grafana as a Prometheus data source, and build a dashboard with at least two panels over the application's metrics. The pass criterion is application metrics from your VMSingle visible on the Grafana graphs.

- [Full statement](TASK.md)
- [Submitted files](submission/) — VM manifests, Grafana dashboard JSON, checks, report
